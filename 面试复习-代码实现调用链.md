# 面试复习 · 数字孪生 SDK 开发者平台（代码实现调用链）

> 目的：按简历每一条要点，把「功能 → 文件 → 函数 → 调用链 → 面试追问」串起来，考前快速过一遍。
> 所有行号基于 2026-08 仓库现状；代码只摘关键片段，完整实现请直接打开对应文件。

---

## 目录

- [0. 项目地图](#0-项目地图)
- [1. 在线调试台（简历第 1 条）](#1-在线调试台简历第-1-条)
- [2. fdapi 智能编辑器（简历第 2 条）](#2-fdapi-智能编辑器简历第-2-条)
- [3. 可视化参数调试（简历第 3 条）](#3-可视化参数调试简历第-3-条)
- [4. AI 代码助手（简历第 4 条）](#4-ai-代码助手简历第-4-条)
- [5. 文档与检索体系（简历第 5 条）](#5-文档与检索体系简历第-5-条)
- [6. DTS MCP Server（简历第 6 条）](#6-dts-mcp-server简历第-6-条)
- [7. 高频面试追问速答](#7-高频面试追问速答)
- [8. 考前速记卡](#8-考前速记卡)

---

## 0. 项目地图

### 0.1 一句话定位

面向 DTS Cloud JavaScript SDK 开发者的一站式开发平台：文档（104 篇）+ 在线调试台（真实连引擎跑代码）+ AI 代码助手 + Skill/MCP 两层 AI 接入，形成「文档查询 → 示例学习 → 在线执行 → AI 生成」闭环。

### 0.2 技术栈

| 层 | 技术 |
| --- | --- |
| 前端框架 | React 18 + Vite 5 + React Router 6 |
| 状态管理 | Zustand（docsStore：sidebarOpen / currentDoc） |
| 样式 | Tailwind CSS 4 + 自定义 playground.css |
| 编辑器 | @uiw/react-codemirror + @codemirror/lang-javascript + one-dark |
| 文档渲染 | react-markdown + remark-gfm + rehype-highlight |
| 搜索 | Fuse.js（本地全文索引） |
| Node/MCP | @modelcontextprotocol/sdk + Puppeteer + zod |
| 数据生成 | Python 脚本（gen_dts.py / gen_param_meta.py / gen_real_examples.py） |
| 运行 SDK | 外部 ac.min.js / ac_conf.js（浏览器端运行时动态加载） |

### 0.3 简历要点 → 核心文件地图

| 简历要点 | 核心文件 |
| --- | --- |
| 1. 在线调试台 | `src/pages/PlaygroundPage.jsx`、`src/utils/sdk.js`、`src/components/Playground/`（ConnectionBar / ExampleNav / PlayerPanel / EditorPanel / ConsolePanel） |
| 2. fdapi 智能编辑器 | `src/components/Playground/EditorPanel.jsx`、`src/data/sandbox/api-completions.js`（生成物） |
| 3. 可视化参数调试 | `src/components/Playground/ParamPanel.jsx`、`src/data/sandbox/param-meta.js`（生成物） |
| 4. AI 代码助手 | `src/components/Playground/AIChat.jsx` |
| 5. 文档与检索 | `src/pages/DocPage.jsx`、`src/hooks/useDocs.js`、`src/utils/search.js`、`src/components/Search/SearchModal.jsx`、`src/components/Markdown/TableOfContents.jsx`、`downloads/dts-spatial-skill/` |
| 6. DTS MCP Server | `downloads/dts-mcp-server/dts-mcp-server/src/index.js`、`doc-parser.js`、`validator.js` |

### 0.4 关键数字（面试必背）

- **69 个 API 命名空间 / 1,237 个函数签名**（1222 个命名空间方法 + 15 个根方法）
- **104 篇文档**（82 API + 22 教程）
- **12 家 LLM 提供商**（Claude / GPT / DeepSeek / 通义千问 / Kimi / GLM / Yi / 百川 / 阶跃 / Gemini / Mistral / Groq）
- **7 个 MCP 工具**（connect / manager_doc / disconnect / status / doc / api_example / call）
- **76 个 API 类索引**（MCP doc-parser 内置 DTS_CLASSES）
- 示例库分**投影 / 球面两套坐标系**（SANDBOX_CATEGORIES / SANDBOX_CATEGORIES_GCS）
- src 目录约 135 个文件、4.5 万行（含生成的数据文件）

---

## 1. 在线调试台（简历第 1 条）

> 简历表述：实现 DTS SDK 动态加载，兼容云渲染视频流与 WebSocket API 两种连接模式，统一管理 SDK 初始化、连接状态、版本校验、异常提示和重连流程；支持 JavaScript、JSON 命令和异步代码执行。

### 1.1 页面结构

```
PlaygroundPage（总控状态）
 ├─ ConnectionBar：模式切换（云渲染/WebSocket）、IP/Port、版本展示、状态灯
 ├─ ExampleNav：投影/球面坐标系切换 + 示例搜索 + 分类树（点击即执行/只填入）
 ├─ PlayerPanel：实时画面（#player 容器 + ParamPanel 浮层）| AI 助手 Tab
 └─ 右侧栏：EditorPanel（CodeMirror）+ 可拖拽分隔条 + ConsolePanel（日志）
```

`App.jsx` 判断 `location.pathname === '/playground'` 时直接渲染 `PlaygroundPage`（不带 Header/Sidebar），整个页面是独立全屏布局。

### 1.2 SDK 动态加载链路（简历要点 1 的前半段）

**调用链**：`PlaygroundPage` 挂载 `useEffect([isCloud])` → `injectGlobalFunctions()` → `loadSdk(baseUrl)` → `loadScript('ac_conf.js')`（可选）→ `loadScript('ac.min.js')` → `initConnection()`。

关键代码（`src/utils/sdk.js:32`）：

```javascript
export async function loadSdk(baseUrl, writeLog) {
    if (!window.__dtsSdkLoaded) {              // 幂等：防重复加载
        try { await loadScript(baseUrl + 'ac_conf.js') } catch {}   // 配置文件可选
        if (!window.HostConfig) {
            window.HostConfig = { API: '127.0.0.1:4321', Player: '127.0.0.1:8889', Path: '' }
        }
        await loadScript(baseUrl + 'ac.min.js')
        window.__dtsSdkLoaded = true
    }
}
```

同时注入示例代码依赖的全局函数（`sdk.js:22`）：`window.log`、`window.logWithColor`、`window.clearScreen`、`window.sleep`、`window.exeFunction`。

失败路径：`ac.min.js` 加载失败 → `setStatus('no-sdk')` + 提示把 SDK 文件复制到 `public/`。

卸载清理：`destroySdk()`（`sdk.js:47`）依次 `fdplayer.destroy()`、`fdapi.destroy()`、清空 `#player` 容器，组件卸载 / 模式切换时执行。

### 1.3 双连接模式（简历要点 1）

`initConnection({ isCloud, apiOptions, ... })`（`sdk.js:57`）二选一：

**云渲染模式（DigitalTwinPlayer）**：
```javascript
window.fdplayer = new window.DigitalTwinPlayer(playerHost, {
    domId: 'player',            // 视频流容器
    iid: urlParams.get('iid'), pid: ..., reset: ..., password: ...,
    apiOptions,                 // onReady / onApiVersion / onLog / onEvent
    events: {
        onVideoLoaded, onConnClose: e => setStatus('error'),
        onRtcStatsReport: stats => setFps(stats.framesPerSecond || 0)
    }
})
window.fdapi = window.fdplayer.getAPI()   // API 总入口
```
host 取 `HostConfig.Player`，若当前页面域名与配置不一致且存在 `HostConfig.PlayerMapping` 则用映射地址。

**WebSocket 仅 API 模式（DigitalTwinAPI）**：
```javascript
window.fdapi = new window.DigitalTwinAPI(HostConfig.API || '127.0.0.1:4321', apiOptions)
```

模式切换：`ConnectionBar` 的 select 改 `isCloud` → `useEffect([isCloud])` 重跑 → 先 `destroySdk()` 再重新加载/初始化。

### 1.4 连接状态机

状态：`loading → connecting → ready / no-sdk / error`

| 状态 | 触发点 |
| --- | --- |
| loading | 初始 |
| connecting | `initConnection()` 开头 `setStatus('connecting')` |
| ready | `apiOptions.onReady`（拿到坐标系类型，写入 `coordType`） |
| no-sdk | `ac.min.js` 加载失败，或两个构造函数都不存在 |
| error | `onConnClose`、`new DigitalTwinPlayer/DigitalTwinAPI` 抛异常 |

`onReady` 里还有一个细节：若存在"文档带入待执行代码"（`pendingAutoRunRef`），则等 300ms 后自动执行，实现"文档 → 调试台一键运行"的体验。

### 1.5 版本校验（简历要点 1）

链路：SDK 触发 `onApiVersion` → 读 `fdapi.misc.apiVersionServer`（服务器文件版本）与 `fdapi.getVersion()`（本地 ac.min.js 版本）→ `fdapi.misc.isApiVersionMatched()` 比对：

- 一致：版本号显示绿色；
- 不一致：显示 `s:<服务端>-c:<客户端>` 红色，并写日志 `ac.min.js 版本和云渲染服务器的文件版本不一致！`。

兜底：若 `window.acapi.VERSION` 存在，则通过 `_onVersion` 直接展示（`sdk.js` 末尾）。

### 1.6 异常提示与重连（简历要点 1）

**异常**：`onConnClose` → `setStatus('error')` + 日志输出关闭 code；执行错误 → `writeLog(e.message)` + `e.stack`。

**重连**（`PlaygroundPage.jsx:322 connectServer`，WebSocket 模式 UI 才显示"连接"按钮）：
```javascript
if (!IP_RE.test(ip) || !/^\+?[1-9][0-9]*$/.test(port)) { /* 格式校验失败 */ }
window.fdapi.destroy()
window.fdapi.setHost(ip, port)
window.fdapi.connectWebSocket()
setStatus('connecting')
```

面试注意：重连是**手动**触发，不要说是"自动重连"。

### 1.7 JS / 异步代码执行（简历要点 1）

**执行链路**：用户点「执行JS」或 Ctrl+Enter → `doExecCode()`（`PlaygroundPage.jsx:280`）：

```javascript
window.eval('(async ()=>{' + codeRef.current + '})()')
```

用 **async IIFE 包裹**，所以示例代码里可以直接写 `await fdapi.camera.set(...)`（顶层 await）。Ctrl+Enter 是 `window.addEventListener('keydown')` 全局监听（`PlaygroundPage.jsx:314`）。

**日志系统**（`writeLog`，`PlaygroundPage.jsx:63`）：`insertAdjacentHTML` 写入 `<pre>`；超过 100 条自动清屏；SDK `onLog` 回调内容先 `escapeHtml` 再写入（**防 XSS**）；`logEnabled` 可全局关日志。

### 1.8 JSON 命令与日志回放（简历要点 1）

**链路**：「执行JSON」→ `doSendJson()`（`PlaygroundPage.jsx:301`）：

1. `matchCmdLog(text)` 用正则 `{"__command.*?__playerId":.*?}` 找出编辑器里所有命令 JSON；
2. 若有多条：解析 `timestamp` → `calcTimeDiffs()` 算相邻时间差 → 循环 `fdapi.call(cmd)` + `await sleep(diff)`，**按原始时间戳间隔回放**（典型场景：回放调试日志里的操作序列）；
3. 若只有一条/没有匹配：`execJson()` 直接 `JSON.parse` + `fdapi.call(o)`。

### 1.9 代码恢复 / 分享 / 自动保存

**恢复优先级**（`PlaygroundPage.jsx:218` 的 effect）：

```
URL hash (#code=base64url) > localStorage.SbPendingCode > localStorage.SbSavedCode > DEFAULT_CODE
```

**分享**（`shareCode`，`PlaygroundPage.jsx:251`）：`TextEncoder` → `btoa` → 把 `+`/`/` 换成 `-_`（URL 安全）→ 拼 `origin + pathname + '#code=' + b64` → 写剪贴板；解码端 `atob` + `TextDecoder`（支持中文/emoji）。

**自动保存**：`useEffect([code])` 500ms 防抖写 `SbSavedCode`。

### 1.10 本模块高频追问

- **为什么要动态加载 SDK？** SDK 属于外部商业包，不入库；运行时从 `public/` 按需注入，还支持通过 URL 参数透传 iid/pid 直连不同实例。
- **两种模式的差异？** 云渲染有视频流（`DigitalTwinPlayer` + `getAPI()`）；WebSocket 模式只有 API 通道（`DigitalTwinAPI`），无画面，适合纯后端联调。
- **eval 安全吗？** 等同浏览器控制台，无沙箱；这是面向开发者自己的调试台，属于产品定位权衡；但 SDK 回调写入 DOM 时做了 escapeHtml。
- **如何做到"文档示例带进调试台自动跑"？** 读 `SbPendingCode` → `setCode` → 置 `pendingAutoRunRef` → `onReady` 后 300ms 自动执行。

---

## 2. fdapi 智能编辑器（简历第 2 条）

> 简历表述：基于 CodeMirror 构建 fdapi 智能编辑器，整合 69 个 API 命名空间和 1,237 个函数签名，实现方法、参数及说明补全，并支持 JavaScript、JSON 命令和异步代码执行。

### 2.1 组件构成（`src/components/Playground/EditorPanel.jsx`）

```javascript
<CodeMirror
    value={code}
    theme={oneDark}
    extensions={[javascript(), fdapiCompletionExt, EditorView.lineWrapping]}
    onChange={val => setCode(val)}
/>
```

其中补全扩展是核心：
```javascript
const fdapiCompletionExt = javascriptLanguage.data.of({ autocomplete: fdapiCompletions })
```

### 2.2 补全调用链（两级正则）

用户输入触发 CodeMirror autocomplete → `fdapiCompletions(context)`（`EditorPanel.jsx:9`）：

**第一级：命名空间下的方法补全**
```javascript
let m = context.matchBefore(/(?:fdapi|api)\.([\w$]+)\.([\w$]*)$/)
// 输入 fdapi.camera.se  → 提取 ns = 'camera' → API_COMPLETIONS.ns['camera']
// options 每个元素：{ label: 方法名, type: 'method', detail: 签名, info: 中文说明 }
```

**第二级：命名空间 + 根方法补全**
```javascript
m = context.matchBefore(/(?:fdapi|api)\.([\w$]*)$/)
// 输入 fdapi.ca → 命名空间列表（camera/cameraTour/...）+ 根方法（getVersion 等 15 个）
```

替换起点：`from = m.from + m.text.lastIndexOf('.') + 1`，`validFor: /^[\w$]*$/`（只对标识符生效，避免在字符串/注释里误触发）。

### 2.3 补全数据从哪来（生成管线）

`src/data/sandbox/api-completions.js` 是**自动生成**的：

```
docs/api/**/*.md（API 文档）
    └─ gen_dts.py 正则解析：
         ① 命名空间：通过 fdapi.xxx 访问
         ② 方法签名：### `methodName(params)`
         ③ 参数表：| param | type | ... |
    └─ 输出 api-completions.js（编辑器补全）+ dts-sdk.d.ts（TypeScript 声明）
```

生成物结构：
```javascript
export const API_COMPLETIONS = {
    root: [ { label: 'getVersion', detail: '(): string', info: '获取版本' }, ... ],  // 15 个
    ns:   { camera: [ { label: 'set', detail: '(options)', info: '...' }, ... ], ... } // 69 个 ns
}
```

**这是"文档驱动"的关键**：新增/修改 API 文档后重跑脚本，编辑器补全自动同步，无需手写。

### 2.4 本模块高频追问

- **补全性能怎么保证？** 数据是静态 JS 对象 + 正则前缀匹配，单次只查一个 ns 的数组，无网络请求；补全在输入停顿才触发（CodeMirror 内置机制）。
- **为什么 detail/info 一起给？** detail 显示参数签名（"参数补全"的本质），info 显示文档说明，让开发者不用切文档。
- **如何保证 1,237 和 69 两个数字真实？** `Object.keys(API_COMPLETIONS.ns).length = 69`，`ns 方法 1222 + root 15 = 1237`。

---

## 3. 可视化参数调试（简历第 3 条）

> 简历表述：自动将 SDK 调用转换为滑块、数值、枚举、颜色等控件；参数修改后实时回写源代码并重新执行，实现代码与可视化调参的双向联动。

### 3.1 整体调用链

```
编辑器代码
  → findAllCalls()     找出所有 fdapi/fdplayer/api 调用（跳过嵌套）
  → parseObject()      解析对象字面量顶层 key:value（含偏移量）
  → buildControl()     合并 PARAM_META 元数据 → 推断控件类型
  → 渲染 lil-gui 风格面板
  → 用户调控件 → applyChange() → serialize() 生成新值文本
  → onTweak(newCode) → setCode() + 250ms 防抖重执行 eval
```

入口在 `PlayerPanel.jsx`：`<ParamPanel code={code} onTweak={onPanelTweak} />` 浮在 `#player` 视频流右上角；`onPanelTweak` 由 `PlaygroundPage.jsx:288` 提供。

### 3.2 代码解析细节（`ParamPanel.jsx`）

- **`matchParen`**（:104）：从 `(` 出发做括号配对，正确处理字符串（`' " \``）、转义、嵌套，返回匹配的 `)` 下标——这是所有解析的地基。
- **`findAllCalls`**（:86）：正则找调用后，用 `re.lastIndex = close + 1` **跳过已匹配调用体**，避免把参数里的嵌套调用重复识别。
- **`parseObject`**（:139）：剥掉 `{ }` 后逐字符扫描，处理带引号的 key、`//` 与 `/* */` 注释、`shorthand`/`spread` 跳过，返回 `{ key, valueText, start, end, comment }`（**偏移量用于回写**）。
- 非对象实参（位置参数）：用文档 `PARAM_META.byNs[ns].methods[method].params` 的名称映射 key，再按顶层逗号切分。

### 3.3 控件类型推断（`buildControl`，:209）

推断优先级（结合文档元数据 + 实参真实值）：

```
1. 文档声明 enum          → 下拉 select（选项来自参数表）
2. 实参是 true/false      → checkbox
3. 实参是 '#hex' / Color.Name / [r,g,b] → 颜色选择器
4. 数值 + 文档有 min/max 或值在 [0,1] → slider（自适应 step）
5. [n,n,n] 2~4 维数组     → 向量多输入框
6. 字符串                → 文本框
7. 其他                 → 不渲染（expr）
```

**防误判细节**：颜色识别要求「键名含 color」或「文档类型为 color」或「归一化 rgb 且文档不是 array」，避免把 `position: [x,y,z]` 坐标误判成颜色。

### 3.4 回写与实时执行（双向联动）

`applyChange`（:305）：`serialize(ctrl, newVal)` 把控件新值转成代码文本（布尔→`true`、颜色→`'#rrggbb'` 或 `[r,g,b]`、向量→`[...]`），然后**按偏移精确替换**：

```javascript
if (code.slice(start, end) !== ctrl.raw) {      // 偏移过期（代码被手动改过）
    const idx = code.indexOf(ctrl.raw)           // 回退：按内容查找
    if (idx < 0) return
    start = idx; end = idx + ctrl.raw.length
}
onTweak(code.slice(0, start) + seg + code.slice(end))
```

`onPanelTweak`（`PlaygroundPage.jsx:288`）：`setCode(newCode)` 更新编辑器，若"立即执行"开启且 `fdapi` 就绪，则 **250ms 防抖**后 `window.eval` 重跑——拖滑块时画面实时变化，这就是"代码 ⇄ 可视化调参双向联动"。

### 3.5 元数据从哪来

`gen_param_meta.py` 解析 docs 参数表 → `param-meta.js`：字段级 `{ t: 类型, d: 默认值, min/max, enum:[{v,label}], desc }` + 方法级 `params` 顺序表；控件按 `byNs[ns].fields[key]` 优先、全局 `fields[key]` 兜底。

### 3.6 本模块高频追问

- **控件类型如何与文档保持一致？** 元数据从文档参数表自动生成，代码里另有启发式推断（值类型、键名、取值范围）兜底。
- **用户手动改了代码，控件偏移失效怎么办？** 替换前校验 `code.slice(start,end) === raw`，不一致则 `indexOf` 内容回退，找不到就放弃本次回写。
- **为什么防抖 250ms？** 滑块拖动高频触发，防抖合并执行，避免每帧都 eval 一次。

---

## 4. AI 代码助手（简历第 4 条）

> 简历表述：通过用户意图匹配、真实函数签名注入和 fdapi 白名单校验，降低 AI 生成代码中的接口幻觉问题。

### 4.1 整体调用链（`AIChat.jsx`）

```
用户输入中文需求
  → send()（:572）
      ├─ 取最近 12 条消息作为上下文
      ├─ system = SYSTEM_PROMPT + buildApiRef(text)   // 按意图注入真实签名
      ├─ 按 provider.format 选择 Anthropic / OpenAI 协议
      ├─ fetch SSE 流式请求（AbortController + 30s 超时）
      ├─ 逐行解析 'data: ' → delta → 追加到消息气泡（流式渲染）
      └─ 流结束后 findInvalidApis(full, validApis) 白名单校验
           ├─ 命中幻觉 API → 拦截，替换为错误提示消息
           └─ 通过 → 显示完整代码 → 用户点「➕ 插入编辑器」→ setCode
```

### 4.2 意图匹配与真实签名注入（`buildApiRef`，:38）

`NS_ALIASES` 把中文意图映射到命名空间：

```javascript
const NS_ALIASES = {
    camera: ['相机', '视角', '镜头', '飞行', '环绕', ...],
    weather: ['天气', '下雨', '雪', '雾', '云', '昼夜', ...],
    marker: ['标注', '标记', '打点', ...],
    // ... 约 30 个命名空间
}
```

匹配逻辑：用户文本命中命名空间名（`ns.toLowerCase()`）或任一中文别名 → 取**最多 5 个**相关命名空间 → 把其中每个方法的**真实签名**（label + detail + 截断 50 字符的说明）拼进 system prompt：

```
以下是与本次问题相关的 API 真实签名，请严格按此参数顺序与类型生成，禁止臆造参数：
【fdapi.weather】
  fdapi.weather.setRainParam({...}) — 雨效参数...
```

只注入命中的命名空间，**控制 token 消耗**；未命中就只给全局清单。SYSTEM_PROMPT 还明确要求：不提供签名的方法，不确定的参数用 `TODO` 注释，**禁止编造**。

### 4.3 多提供商与流式请求

- 12 家提供商统一为 `{ id, name, models[], apiBase/endpoint, format }`，`format: 'anthropic' | 'openai'` 两种协议；
- Anthropic：`POST /v1/messages`，Header 带 `x-api-key` + `anthropic-version` + `anthropic-dangerous-direct-browser-access`；
- OpenAI 兼容：`POST /v1/chat/completions`，`Authorization: Bearer`；
- 浏览器**直连**厂商 API，Key 只存 `localStorage`，不过中间服务器（面试安全点）；
- 流式解析：`reader.read()` → `TextDecoder({stream:true})` → 按行找 `data: ` → 取 `delta.text` / `choices[0].delta.content` → 实时 `setMessages`。

### 4.4 fdapi 白名单校验（防幻觉核心）

**白名单构建**（`buildCompletionApis` :65 + `fetchValidApis` :79）：

```javascript
const set = buildCompletionApis()          // ① 权威：API_COMPLETIONS（文档全集）
const resp = await fetch('/api_examples.js') // ② 补充：官方示例里的 fdapi 调用
for (const m of resp.text().match(/fdapi(?:\.\w+)+/g) || []) set.add(m)
```

**校验**（`findInvalidApis` :99）：
```javascript
const calls = responseText.match(/fdapi(?:\.\w+)+/g) || []   // 提取生成代码里所有 fdapi 调用
return [...new Set(calls.filter(c => !whitelist.has(c)))]     // 不在白名单 = 幻觉
```

流式结束后执行；有幻觉 API 就把消息替换成：`⚠️ 生成代码中包含 SDK 文档里不存在的 API：xxx，该响应已被拦截`（error 消息）。

细节：白名单未加载完成时**放行**（避免误伤）；HTTP 401 自动清除对应 Key。

### 4.5 Key / 模型管理

localStorage 键：`DtsAiProvider`、`DtsAiKey_{providerId}`、`DtsAiModel_{providerId}`（自定义模型存 `__c:模型名`）；配置面板分「主要」4 家 Tab + 「其他」国内/国际分组。

### 4.6 本模块高频追问

- **为什么注入真实签名能降幻觉？** 幻觉主要来自模型不知道确切参数；把文档里的权威签名直接放 system prompt，模型按给定参数顺序/类型生成，把"回忆"变成"填空"。
- **白名单为什么以文档为权威？** 文档全集覆盖全部公开 API；`api_examples.js` 补充文档没覆盖但确实存在的内部示例调用，双源并集。
- **流式中途怎么停止？** `AbortController` + `abortRef`，「停止」按钮触发 `abort()`。
- **隐私/安全？** Key 只存本地，直连厂商，无中间代理；这既是优点（简单）也是限制（无法做服务端限流/审计）。

---

## 5. 文档与检索体系（简历第 5 条）

> 简历表述：沉淀 104 篇教程/API 文档，支持全文搜索、代码高亮、页内导航及示例检索；将 SDK 知识封装为 Skill，建立类、方法、教程和类型级查询路由。

### 5.1 文档渲染链路

```
main.jsx (BrowserRouter)
  → App.jsx 路由 /docs/:category/*
  → DocLayout (Sidebar + DocPage + Footer)
  → DocPage.jsx
      → useDoc(category, slug)（hooks/useDocs.js:233）
          ├─ import.meta.glob('../data/docs/**/*.md', { query:'?raw' })  // Vite 静态导入
          ├─ resolveDocPath(category, slug) → loader()
          └─ stripFrontmatter（正则去掉 --- yaml ---）→ setContent
      → <ReactMarkdown remarkPlugins={[remarkGfm, remarkHeadingIds]}
                       rehypePlugins={[rehypeHighlight]}>
```

- `remarkGfm`：表格/删除线等 GFM 语法；
- `rehypeHighlight`：highlight.js 代码高亮；
- `remarkHeadingIds`（`src/utils/remark-heading-ids.js`）：支持 `{#custom-id}` 自定义锚点语法；
- `components` 覆写 `<a>`：`#` 开头 → 平滑滚动 + `history.replaceState`；`/` 开头站内链接；其余外链新窗口。

### 5.2 全文搜索（Fuse.js）

链路：`Header` 的 `SearchTrigger` 打开 `SearchModal` → 输入防抖 **200ms** → `search(query)`（`utils/search.js:113`）→ 首次懒构建索引 → 结果渲染 → ↑↓/Enter 键盘导航 → `navigate('/docs/:category/:slug')`。

**索引构建**（`buildIndex` :73）：
- `flattenDocMeta()` 把 docMeta 拍平为扁平条目；
- 并行 `Promise.all` 加载全部 md → `stripFrontmatter` + `stripMarkdown`（去掉代码块/行内码/标题/链接/列表标记）得到纯文本；
- `new Fuse(entries, { keys: [title 0.4, sidebar_label 0.3, content 0.2, groupLabel 0.1], threshold: 0.35, includeMatches: true })`。

**摘要**（`buildSnippet` :135）：取第一个 `content` 命中位置，前后各扩展 20/60 字符，返回 `{ text, highlights }`，前端对高亮区间做标记。

示例库还有自己的侧边栏搜索（`ExampleNav` 的 `searchQuery` 过滤类/方法），这就是简历里的"示例检索"。

### 5.3 页内导航（TableOfContents）

`TableOfContents.jsx`：文档渲染后扫描 `.prose` 容器里的 `h2/h3` → 从文本生成稳定 id（含中文 `[\w一-鿿]` 正则）→ `IntersectionObserver`（rootMargin -80px/-70%）跟踪当前章节高亮 → 点击平滑滚动到锚点。

### 5.4 Skill 封装（`downloads/dts-spatial-skill/`）

结构：`SKILL.md`（主入口）+ `api/<分类>/*.md` + `tutorials/*.md` + `api/types.md` + `api/events/`。

查询路由（SKILL.md 约定）：

```
1. 某个类/方法（"Camera 怎么飞入场景"）→ api/<分类>/<文件>.md（类索引定位）
2. 入门/概念（异步/事件/坐标/颜色）   → tutorials/<主题>.md（教程索引）
3. 枚举/全局类型（Color/ActionMode）  → api/types.md
4. 事件回调（onEvent 收到的事件）     → api/events/<文件>.md
5. 中文功能词 → 功能→类 / 方法→类 映射先定位类，再走 1
```

SKILL.md 还内置初始化模板、调用约定（`onReady` 后才能调 API、`updateBegin/update/updateEnd` 批量更新、坐标/颜色约定）与常用代码模板（相机定位、添加 Marker、批量更新）。**面试点**：这相当于把静态文档改造成了"AI 可检索的知识库"，是 MCP 之外的另一种 AI 接入形态。

### 5.5 本模块高频追问

- **为什么用 Fuse.js 而不是服务端搜索？** 文档全量本地静态托管，Fuse 支持权重与模糊匹配，零后端依赖，秒级出结果；文档规模（104 篇）完全可接受。
- **中文分词怎么办？** Fuse 按字符匹配 + 降低 threshold，对中文标题/正文仍可用；配合权重字段排序。
- **Skill 和 MCP 的区别？** Skill 是"给 AI 读的知识包"（查资料）；MCP 是"给 AI 用的工具"（能连真机执行）。两者解决不同问题。

---

## 6. DTS MCP Server（简历第 6 条）

> 简历表述：解决浏览器端 SDK 无法直接在 Node.js 调用的问题，封装连接、状态、文档、示例及 API 执行等 7 个 MCP 工具，实现 AI 客户端对数字孪生场景的操控。

### 6.1 架构

```
┌────────────┐  MCP/stdio   ┌───────────────────────────────┐
│  AI 客户端  │ ───────────▶ │  dts-mcp-server (Node)        │
│ (Claude等) │ ◀─────────── │  src/index.js（7 个工具）      │
└────────────┘   工具结果     └──────┬───────────────────────┘
                                    │
                ┌───────────────────┼───────────────────┐
           REST /manage/*      Puppeteer 无头浏览器    按需抓取文档
           (实例列表/空闲)      DigitalTwinPlayer      /locale/zh/doc/*.html
                                    → fdapi            manager.html / api_examples.js
```

**核心问题**：DTS SDK（DigitalTwinPlayer）只能在浏览器运行，Node 后端无法 `require` 直接调。方案：**Puppeteer 起无头浏览器加载 SDK 建立连接，把 AI 指令转发到页面里的 fdapi 执行**——MCP Server 成为"AI ⇄ 数字孪生引擎"的桥。

### 6.2 连接链路（dts_connect）

```
dts_connect({host, port, iid?, autoSelect?})
  ├─ 已连接同服务器 → 直接返回 Already connected
  ├─ getInstanceList()：REST GET /manage/instance（兼容多响应格式）
  │    ├─ 0 个实例 → 报错提示先启动实例
  │    ├─ 1 个实例 → 自动选用
  │    └─ 多个 + autoSelect → /manage/instance/free 挑空闲，失败则回退运行中实例
  └─ browserConnect()（index.js:146）
       ├─ detectBrowserPath()：环境变量 → 系统常见路径（Edge/Chrome/Brave）→ which
       ├─ puppeteer.launch({ headless:'new', args:[--no-sandbox ...] })
       ├─ goto 服务器（302 → /locale/zh/player.html）
       ├─ addScriptTag('/libac') 注入 SDK
       ├─ evaluate：new DigitalTwinPlayer(addr, { iid, pid, apiOptions.onReady → _dtsReady=true })
       ├─ waitForFunction(_dtsReady) × 3 次重试（应对慢启动实例）
       └─ 注入 window._dtsCall 辅助函数（统一回调风格 + JSON 序列化）
```

### 6.3 文档 / 示例 / 管理接口链路

- **dts_doc**：不传 class → `formatClassList()` 输出 76 个类索引；否则 `fetchClassDoc()`：REST 抓 `/locale/zh/doc/{Class}.html` → `parseDocHtml()` 按 JSDoc HTML 结构拆分（`<h4 id="方法名">` 签名、description、参数表、`<pre>` 示例）→ 缓存进 `docIndex`；`dts_doc({class, method})` 再输出 `formatMethodHelp()`（含必填参数、嵌套属性、RGBA 颜色提醒）。
- **dts_api_example**：抓 `/locale/zh/scripts/api_examples.js` → `extractApiExample()` 按 `test_{类名小写}_{方法名}` 函数名正则 + **花括号计数**截取完整函数体；查不到时列相近可用方法做提示。
- **dts_manager_doc**：抓 `/locale/zh/manager.html` → `parseManagerHtml()` 解析 `#interfaceList` 表格 → 按模块分组输出 REST/WebSocket/是否登录，支持 `search` 过滤。

### 6.4 执行链路（dts_call）

```
dts_call({class, method, params})
  ├─ 有文档 → validateParams()：必填顶层参数 + data 对象嵌套必填属性
  ├─ page.evaluate(window._dtsCall, ns, method, params)
  │     ├─ params.data → api[method](data, cb)
  │     ├─ 多个位置参数 → Object.values(params) + cb
  │     └─ 无参数 → api[method](cb)
  │     回调是单参 result（DTS 风格，非 Node err-first）
  └─ 结果 JSON.stringify（预览截断 30000 字符）
```

参数约定（README 明示，也是 AI 被教导的规则）：增改类方法负载放 `data` 键；位置参数按**对象键的插入顺序**传；颜色一律 `[R,G,B,A]` 0~1 数组，不要用 `Color.Red` 字符串。

### 6.5 缓存与健壮性

- `docIndex` / `examplesCache` 带 `docCacheServer` / `examplesCacheServer`，**切换目标服务器自动清空**（修过陈旧缓存的 bug）；
- 连接 3 次重试 + 失败时输出连接调试详情（hasPlayer/hasAPI/ready/error）；
- `restGet` 10s 超时；`dts_call` 前文档抓取失败不阻断执行（校验是尽力而为）；
- 已知限制：位置参数依赖键序；管理接口登录鉴权未实现；HTML 正则解析对文档结构变化敏感。

### 6.6 本模块高频追问

- **为什么必须用无头浏览器？** SDK 本质是浏览器端 JS（依赖 DOM/视频流能力），Node 环境无法直接实例化；Puppeteer 提供一个"浏览器运行时"来承载 SDK。
- **无头浏览器开销大吗？** 每个连接一个浏览器实例，主要用于 AI 会话级操作；`dts_disconnect` / 服务器切换会关闭浏览器释放资源。
- **AI 怎么知道调用哪些方法？** dts_doc / dts_api_example 就是"先查文档再看示例再执行"的标准链路，README 里也给了典型对话流程。

---

## 7. 高频面试追问速答

**Q1：为什么用 eval 执行用户代码？安全吗？**
答：调试台本质是"浏览器控制台"，用 async IIFE 包裹以支持顶层 await，这是产品定位（给开发者调试 SDK 用）。风险点：SDK 回调日志写入 DOM 前做 escapeHtml 防 XSS；URL hash 分享的代码只在用户自己浏览器执行。若要更安全可换 iframe + postMessage 沙箱（已知改进方向）。

**Q2：编辑器补全的 1,237 个签名怎么保证和文档一致？**
答：不手写。`gen_dts.py` 从 104 篇 Markdown 文档正则解析命名空间/方法签名/参数表，自动生成 api-completions.js 和 dts-sdk.d.ts；文档改 → 重跑脚本 → 补全同步。

**Q3：参数面板回写代码时，偏移量过期了怎么办？**
答：`applyChange` 先校验 `code.slice(start,end) === ctrl.raw`，不一致就 `indexOf(raw)` 按内容回退查找；找不到（例如被删掉）直接放弃，保证不写坏代码。

**Q4：AI 幻觉接口怎么拦截？**
答：三层防线：① 意图→命名空间匹配后注入真实签名（减少编造空间）；② 流式结束后用「文档全集 ∪ api_examples.js」白名单正则提取生成代码里的 fdapi 调用逐一比对；③ 命中幻觉整条响应替换为错误提示。

**Q5：为什么 MCP Server 要启动无头浏览器？**
答：DTS SDK（DigitalTwinPlayer）只能在浏览器运行，Node 直接 require 会因缺少 DOM/视频能力失败。用 Puppeteer 加载 SDK 连上云渲染实例后，AI 的 `dts_call` 通过 `page.evaluate` 转发到 `fdapi` 执行，等于给 AI 造了一个"能操作三维场景的浏览器运行时"。

**Q6：版本校验不一致会怎样？**
答：`onApiVersion` 里比对 `fdapi.misc.apiVersionServer` 与 `fdapi.getVersion()`，不一致时连接栏显示 `s:服务端-c:客户端` 红色并打日志提醒，但**不阻断使用**——提示开发者同步 SDK 文件，避免接口差异导致调用失败。

**Q7：给平台新增一个 API 类，要动哪些地方？**
答：只需在 docs 里新增 Markdown 文档（按统一模板：方法/参数表/示例），然后：重跑生成脚本同步补全、参数元数据、示例库；Skill 的类索引补一行；MCP 的 `DTS_CLASSES` 可补类描述。**前端渲染、补全、调参面板零改动**——这就是文档驱动架构的收益。

**Q8：搜索性能/体验怎么做的？**
答：Fuse.js 本地索引懒加载（首次搜索才建），字段加权排序（标题 > 侧边栏名 > 正文 > 分组），命中片段截取 + 高亮坐标，输入 200ms 防抖 + 键盘导航（↑↓ 选择、Enter 跳转、Esc 关闭）。

**Q9：Skill 和 MCP Server 都是给 AI 用的，区别是什么？**
答：Skill 是**知识**（把 104 篇文档拆成类/方法/教程/类型四级路由，AI 按需读取，解决"不知道 API 怎么用"）；MCP 是**能力**（7 个工具连真机，解决"能查能跑"）。两者互补：Skill 让 AI 回答准确，MCP 让 AI 行动落地。

**Q10：项目里最难的坑是什么？**
答：可选回答方向：① 浏览器端 SDK 与 Node 的鸿沟（MCP 用无头浏览器桥接）；② 参数回写与代码编辑的偏移一致性（内容回退查找）；③ 文档驱动数据的自动生成管线（正则解析脆弱，但换来自动化）；④ AI 幻觉治理（意图注入 + 白名单双保险）。

---

## 8. 考前速记卡

**数字**：69 ns / 1237 签名 / 104 篇文档（82+22）/ 12 家 LLM / 7 个 MCP 工具 / 76 个类索引。

**三句话讲清项目**：
1. 一个给 DTS 数字孪生 SDK 开发者用的文档 + 调试平台：104 篇文档、CodeMirror 智能补全（69 命名空间/1237 签名）、可视化调参面板、12 家 LLM 的 AI 代码助手；
2. 所有编辑器/调参数据由 Python 脚本从文档自动生成，形成"文档驱动"的维护模式；
3. 面向 AI 做了两层输出：Skill 知识包（四级查询路由）和 MCP Server（Puppeteer 无头浏览器桥接，7 个工具让 AI 直接操控三维场景）。

**核心文件一句话索引**：

| 想讲什么 | 打开 |
| --- | --- |
| 连接/加载/状态机 | `src/utils/sdk.js` |
| 调试台总控（执行/回放/分享/重连） | `src/pages/PlaygroundPage.jsx` |
| 补全 | `EditorPanel.jsx` + `api-completions.js` |
| 参数面板 | `ParamPanel.jsx` + `param-meta.js` |
| AI 助手 | `AIChat.jsx` |
| 文档加载/渲染 | `useDocs.js` + `DocPage.jsx` |
| 搜索 | `search.js` + `SearchModal.jsx` |
| MCP | `downloads/dts-mcp-server/.../src/index.js` |

**考前自检**：能否不看代码，徒手画出「点示例 → 执行 → 参数面板联动」和「AI 提问 → 注入签名 → 流式返回 → 白名单校验」两条链路？能，就稳了。
