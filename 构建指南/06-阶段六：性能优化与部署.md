# 阶段六：性能优化与部署

## 📋 阶段概述

**目标**：优化性能、响应式设计、部署上线
**时间**：2天
**难度**：⭐⭐⭐

---

## 一、阶段目标

### 1.1 主要目标

- ✅ 实现主题切换（暗色/亮色）
- ✅ 实现响应式布局
- ✅ 实现性能优化
- ✅ 实现离线支持
- ✅ 部署到生产环境

### 1.2 验收标准

- [ ] 暗色/亮色主题可切换
- [ ] 移动端可正常访问
- [ ] 页面加载速度快
- [ ] 离线可访问（可选）
- [ ] 生产环境可正常运行

---

## 二、任务清单

### 2.1 主题系统

- [ ] 实现主题切换功能
- [ ] 实现系统偏好检测
- [ ] 实现主题持久化

### 2.2 响应式设计

- [ ] 移动端适配
- [ ] 侧边栏折叠
- [ ] 触摸手势支持

### 2.3 性能优化

- [ ] 代码分割
- [ ] 懒加载
- [ ] 虚拟滚动（可选）
- [ ] 缓存策略

### 2.4 部署准备

- [ ] 构建优化
- [ ] 环境变量配置
- [ ] 部署脚本

---

## 三、详细步骤

### 3.1 安装依赖

```bash
# 无额外依赖
```

### 3.2 创建目录

```bash
mkdir -p src/hooks
mkdir -p src/styles
```

---

## 四、核心代码实现

### 4.1 主题切换Hook

```javascript
// src/hooks/useTheme.js
import { useState, useEffect } from 'react';

const useTheme = () => {
  const [theme, setTheme] = useState(() => {
    // 从localStorage读取
    const saved = localStorage.getItem('theme');
    if (saved) return saved;
    
    // 检测系统偏好
    return window.matchMedia('(prefers-color-scheme: dark)').matches
      ? 'dark'
      : 'light';
  });
  
  // 应用主题
  useEffect(() => {
    document.documentElement.setAttribute('data-theme', theme);
    localStorage.setItem('theme', theme);
  }, [theme]);
  
  // 监听系统偏好变化
  useEffect(() => {
    const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
    
    const handleChange = (e) => {
      if (!localStorage.getItem('theme')) {
        setTheme(e.matches ? 'dark' : 'light');
      }
    };
    
    mediaQuery.addEventListener('change', handleChange);
    return () => mediaQuery.removeEventListener('change', handleChange);
  }, []);
  
  // 切换主题
  const toggleTheme = () => {
    setTheme(prev => prev === 'dark' ? 'light' : 'dark');
  };
  
  return { theme, toggleTheme };
};

export default useTheme;
```

### 4.2 主题切换组件

```jsx
// src/components/UI/ThemeToggle.jsx
import { Sun, Moon } from 'lucide-react';
import useTheme from '../../hooks/useTheme';

const ThemeToggle = () => {
  const { theme, toggleTheme } = useTheme();
  
  return (
    <button
      onClick={toggleTheme}
      className="p-2 rounded-lg hover:bg-gray-700 transition-colors"
      title={theme === 'dark' ? '切换到亮色模式' : '切换到暗色模式'}
    >
      {theme === 'dark' ? (
        <Sun size={20} className="text-yellow-400" />
      ) : (
        <Moon size={20} className="text-gray-400" />
      )}
    </button>
  );
};

export default ThemeToggle;
```

### 4.3 响应式布局Hook

```javascript
// src/hooks/useResponsive.js
import { useState, useEffect } from 'react';

const useResponsive = () => {
  const [windowSize, setWindowSize] = useState({
    width: window.innerWidth,
    height: window.innerHeight,
  });
  
  const [isMobile, setIsMobile] = useState(window.innerWidth < 768);
  const [isTablet, setIsTablet] = useState(window.innerWidth >= 768 && window.innerWidth < 1024);
  const [isDesktop, setIsDesktop] = useState(window.innerWidth >= 1024);
  
  useEffect(() => {
    const handleResize = () => {
      const width = window.innerWidth;
      const height = window.innerHeight;
      
      setWindowSize({ width, height });
      setIsMobile(width < 768);
      setIsTablet(width >= 768 && width < 1024);
      setIsDesktop(width >= 1024);
    };
    
    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);
  
  return {
    windowSize,
    isMobile,
    isTablet,
    isDesktop,
  };
};

export default useResponsive;
```

### 4.4 移动端侧边栏

```jsx
// src/components/Layout/MobileSidebar.jsx
import { useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import { Menu, X } from 'lucide-react';
import useDocsStore from '../../stores/docsStore';

const MobileSidebar = () => {
  const [isOpen, setIsOpen] = useState(false);
  const { slug } = useParams();
  const { getDocsByCategory } = useDocsStore();
  
  const tutorials = getDocsByCategory('tutorials');
  const apiDocs = getDocsByCategory('api');
  
  const handleLinkClick = () => {
    setIsOpen(false);
  };
  
  return (
    <>
      {/* 菜单按钮 */}
      <button
        onClick={() => setIsOpen(true)}
        className="lg:hidden fixed top-4 left-4 z-50 p-2 bg-gray-800 rounded-lg"
      >
        <Menu size={24} className="text-white" />
      </button>
      
      {/* 遮罩 */}
      {isOpen && (
        <div
          className="fixed inset-0 bg-black bg-opacity-50 z-40 lg:hidden"
          onClick={() => setIsOpen(false)}
        />
      )}
      
      {/* 侧边栏 */}
      <aside
        className={`fixed top-0 left-0 h-full w-64 bg-gray-800 z-50 transform transition-transform duration-300 lg:translate-x-0 ${
          isOpen ? 'translate-x-0' : '-translate-x-full'
        }`}
      >
        {/* 关闭按钮 */}
        <button
          onClick={() => setIsOpen(false)}
          className="absolute top-4 right-4 text-gray-400 hover:text-white lg:hidden"
        >
          <X size={24} />
        </button>
        
        {/* Logo */}
        <div className="p-4 border-b border-gray-700">
          <Link to="/" className="text-xl font-bold text-white" onClick={handleLinkClick}>
            DTS SDK
          </Link>
        </div>
        
        {/* 导航 */}
        <nav className="p-4 overflow-y-auto h-[calc(100-4rem)]">
          {/* 教程 */}
          <div className="mb-6">
            <h3 className="text-lg font-semibold mb-3 text-cyan-400">开发教程</h3>
            <ul>
              {tutorials.map((doc) => (
                <li key={doc.slug} className="mb-1">
                  <Link
                    to={`/docs/${doc.slug}`}
                    onClick={handleLinkClick}
                    className={`block p-2 rounded text-sm ${
                      slug === doc.slug
                        ? 'bg-cyan-600 text-white'
                        : 'hover:bg-gray-700'
                    }`}
                  >
                    {doc.title}
                  </Link>
                </li>
              ))}
            </ul>
          </div>
          
          {/* API文档 */}
          <div>
            <h3 className="text-lg font-semibold mb-3 text-cyan-400">API 文档</h3>
            <ul>
              {apiDocs.map((doc) => (
                <li key={doc.slug} className="mb-1">
                  <Link
                    to={`/docs/${doc.slug}`}
                    onClick={handleLinkClick}
                    className={`block p-2 rounded text-sm ${
                      slug === doc.slug
                        ? 'bg-cyan-600 text-white'
                        : 'hover:bg-gray-700'
                    }`}
                  >
                    {doc.title}
                  </Link>
                </li>
              ))}
            </ul>
          </div>
        </nav>
      </aside>
    </>
  );
};

export default MobileSidebar;
```

### 4.5 更新Layout组件

```jsx
// src/components/Layout/Layout.jsx
import { Outlet } from 'react-router-dom';
import Header from './Header';
import Sidebar from './Sidebar';
import MobileSidebar from './MobileSidebar';
import Footer from './Footer';
import useResponsive from '../../hooks/useResponsive';

const Layout = () => {
  const { isMobile } = useResponsive();
  
  return (
    <div className="min-h-screen flex flex-col">
      <Header />
      <div className="flex flex-1">
        {isMobile ? <MobileSidebar /> : <Sidebar />}
        <main className="flex-1 p-4 lg:p-6">
          <Outlet />
        </main>
      </div>
      <Footer />
    </div>
  );
};

export default Layout;
```

### 4.6 更新Header组件

```jsx
// src/components/Layout/Header.jsx
import { Link } from 'react-router-dom';
import SearchTrigger from '../Search/SearchTrigger';
import ThemeToggle from '../UI/ThemeToggle';

const Header = () => {
  return (
    <header className="bg-gray-900 text-white p-4 sticky top-0 z-40">
      <div className="container mx-auto flex justify-between items-center">
        <Link to="/" className="text-xl font-bold">
          DTS Cloud SDK
        </Link>
        
        <div className="flex items-center space-x-4">
          <SearchTrigger />
          <ThemeToggle />
          <nav className="hidden md:block">
            <ul className="flex space-x-4">
              <li><Link to="/" className="hover:text-cyan-400">首页</Link></li>
              <li><Link to="/docs/hello-world" className="hover:text-cyan-400">文档</Link></li>
              <li><Link to="/playground" className="hover:text-cyan-400">在线调试</Link></li>
            </ul>
          </nav>
        </div>
      </div>
    </header>
  );
};

export default Header;
```

### 4.7 性能优化

```javascript
// src/App.jsx
import { lazy, Suspense } from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Layout from './components/Layout/Layout';

// 懒加载页面
const Home = lazy(() => import('./pages/Home'));
const DocPage = lazy(() => import('./pages/DocPage'));
const PlaygroundPage = lazy(() => import('./pages/PlaygroundPage'));

// 加载组件
const Loading = () => (
  <div className="flex items-center justify-center h-screen">
    <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-cyan-400"></div>
  </div>
);

function App() {
  return (
    <BrowserRouter>
      <Suspense fallback={<Loading />}>
        <Routes>
          <Route path="/" element={<Layout />}>
            <Route index element={<Home />} />
            <Route path="docs/:slug" element={<DocPage />} />
            <Route path="playground" element={<PlaygroundPage />} />
          </Route>
        </Routes>
      </Suspense>
    </BrowserRouter>
  );
}

export default App;
```

### 4.8 缓存Hook

```javascript
// src/hooks/useCache.js
import { useState, useEffect } from 'react';

const useCache = (key, fetcher, options = {}) => {
  const { ttl = 5 * 60 * 1000 } = options; // 默认5分钟缓存
  
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);
  
  useEffect(() => {
    const loadData = async () => {
      try {
        // 检查缓存
        const cached = localStorage.getItem(key);
        if (cached) {
          const { data: cachedData, timestamp } = JSON.parse(cached);
          if (Date.now() - timestamp < ttl) {
            setData(cachedData);
            setLoading(false);
            return;
          }
        }
        
        // 获取新数据
        const newData = await fetcher();
        setData(newData);
        
        // 保存到缓存
        localStorage.setItem(key, JSON.stringify({
          data: newData,
          timestamp: Date.now(),
        }));
      } catch (err) {
        setError(err);
      } finally {
        setLoading(false);
      }
    };
    
    loadData();
  }, [key, fetcher, ttl]);
  
  // 清除缓存
  const clearCache = () => {
    localStorage.removeItem(key);
    setData(null);
  };
  
  return { data, loading, error, clearCache };
};

export default useCache;
```

### 4.9 更新样式

```css
/* src/styles/global.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 主题变量 */
:root {
  --bg-primary: #0a0e14;
  --bg-secondary: #1a1f2e;
  --text-primary: #e6f0ff;
  --text-secondary: #8595a8;
  --accent-color: #22d3ee;
}

[data-theme="light"] {
  --bg-primary: #ffffff;
  --bg-secondary: #f3f4f6;
  --text-primary: #1a1a1a;
  --text-secondary: #6b7280;
  --accent-color: #0891b2;
}

/* 全局样式 */
body {
  background-color: var(--bg-primary);
  color: var(--text-primary);
  transition: background-color 0.3s, color 0.3s;
}

/* 代码块样式 */
.code-block {
  border-radius: 0.5rem;
  overflow: hidden;
}

.code-header {
  background-color: var(--bg-secondary);
}

pre {
  background-color: var(--bg-secondary);
}

/* 响应式调整 */
@media (max-width: 768px) {
  .sidebar {
    display: none;
  }
  
  .toc {
    display: none;
  }
}

/* 滚动条样式 */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: var(--bg-secondary);
}

::-webkit-scrollbar-thumb {
  background: var(--text-secondary);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--accent-color);
}
```

### 4.10 环境变量配置

```bash
# .env
VITE_APP_TITLE=DTS Cloud SDK
VITE_APP_API_BASE=https://api.example.com

# .env.production
VITE_APP_TITLE=DTS Cloud SDK
VITE_APP_API_BASE=https://api.dtscloud.com
```

### 4.11 构建脚本

```json
// package.json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .js,.jsx",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

### 4.12 部署配置

```javascript
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/', // 部署到根目录
  build: {
    outDir: 'dist',
    sourcemap: false,
    minify: 'terser',
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom', 'react-router-dom'],
          markdown: ['react-markdown', 'remark-gfm', 'rehype-highlight'],
        },
      },
    },
  },
});
```

---

## 五、部署指南

### 5.1 GitHub Pages部署

```bash
# 1. 安装gh-pages
npm install -D gh-pages

# 2. 配置package.json
# 添加 "homepage": "https://username.github.io/repo-name"

# 3. 部署
npm run deploy
```

### 5.2 Vercel部署

```bash
# 1. 安装Vercel CLI
npm install -g vercel

# 2. 登录
vercel login

# 3. 部署
vercel
```

### 5.3 Netlify部署

```bash
# 1. 构建
npm run build

# 2. 上传dist目录到Netlify
```

---

## 六、测试验证

### 6.1 启动项目

```bash
npm run dev
```

### 6.2 验证功能

1. **主题切换**
   - 点击主题切换按钮
   - 暗色/亮色模式正常切换
   - 刷新后主题保持

2. **响应式布局**
   - 调整浏览器窗口大小
   - 移动端显示移动侧边栏
   - 桌面端显示桌面侧边栏

3. **性能**
   - 页面加载速度快
   - 代码分割正常工作
   - 懒加载正常工作

4. **生产环境**
   - `npm run build` 成功
   - `npm run preview` 可预览
   - 部署后可正常访问

---

## 七、常见问题

### Q1: 主题切换不生效
A: 检查CSS变量是否正确应用，检查localStorage是否正常工作

### Q2: 移动端侧边栏不显示
A: 检查响应式断点，检查z-index层级

### Q3: 构建失败
A: 检查依赖是否完整，检查代码是否有语法错误

### Q4: 部署后页面空白
A: 检查base路径配置，检查路由配置

---

## 八、项目完成

恭喜！你已经完成了DTS Cloud数字孪生平台API文档系统的所有功能开发。

### 项目总结

- ✅ 文档渲染系统
- ✅ 导航与搜索系统
- ✅ 在线调试系统
- ✅ AI代码助手
- ✅ 性能优化
- ✅ 响应式设计
- ✅ 主题切换
- ✅ 部署上线

### 简历亮点

```
项目名称：DTS Cloud 数字孪生平台 API 文档系统
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

技术栈：React 18 + Vite + Zustand + Tailwind CSS

核心功能：
1. 文档渲染引擎 - 自定义Markdown渲染器，支持GFM语法、代码高亮、API参数表格
2. 在线调试系统 - 浏览器端JavaScript沙箱，支持实时API调用和控制台输出
3. AI代码助手 - 多模型集成，API白名单验证，流式代码生成
4. 搜索系统 - 基于Fuse.js的本地全文搜索，支持中文分词

技术难点：
• 实现安全的代码沙箱执行环境
• 优化AI代码生成的准确性和响应速度
• 解决大型Markdown文档的渲染性能问题
• 实现本地搜索的中文分词支持
```

---

**文档版本**：v1.0  
**最后更新**：2026年6月23日
